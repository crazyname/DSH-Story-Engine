import { CHANNELS,MESSAGES,PARTICIPANTS,STORY_FRAME } from './mock-data.ts'
import type { StorySaveProjection } from './story-domain.ts'
export function createInitialProjection():StorySaveProjection{return{schemaVersion:1,saveId:'lantern-demo-save',packId:'lantern-station',packTitle:'雾海灯塔站',selectedChannelId:CHANNELS.find(c=>c.pinned)?.id??CHANNELS[0]!.id,participants:PARTICIPANTS.map(p=>({...p,aliases:[]})),channels:CHANNELS.map(c=>({...c,muted:false,archived:false})),messages:MESSAGES.map((m,index)=>({...m,createdAt:`2026-08-28T${m.createdAt}:00.000+08:00`,seasonId:'S1',episodeId:'E1',sceneId:STORY_FRAME.sceneLabel,turnId:`fixture-${index+1}`,canonStatus:'committed' as const})),drafts:{},readCursors:{},frame:{seasonLabel:STORY_FRAME.seasonLabel,episodeLabel:STORY_FRAME.episodeLabel,sceneLabel:STORY_FRAME.sceneLabel},revision:0,updatedAt:'2026-08-28T19:00:00.000+08:00'}}
